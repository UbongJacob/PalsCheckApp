import React from 'react';
import {StyleSheet} from 'react-native';
import AppText from './appText';

const Post = () => {
  return <AppText>Post Component</AppText>;
};

const styles = StyleSheet.create({});

export default Post;
